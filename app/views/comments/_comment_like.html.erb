<% liked = comment.likes.find_by_user_id(current_user.id).present? %>
<% if liked %>
  <%= link_to 'unlike', like_comment_path(id: comment.id), remote: true %>
<% else %>
  <%= link_to 'like', like_comment_path(id: comment.id), remote: true %>
<% end %>
(<%= comment.likes.count %>)