
if (<%= @post.errors.any? %>) {
  $("#error").html("<%= @post.errors.full_messages.first %>");  
} else {
$(".posts").prepend("<%= escape_javascript render(partial: 'post', locals:{post: @post}) %>");
}
$("#counting").val('');